declare module 'better-sqlite3' {
  // Minimal typing for Next.js build to accept server-side usage.
  // For more accurate typings, replace `any` with concrete types or install @types if available.
  const Database: any;
  export default Database;
}
